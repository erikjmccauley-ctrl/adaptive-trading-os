# scripts/dev

Local development entrypoints.

## Files (to be created during file migration phase)
- `run_local.py` — current `main.py` (30s live scanner, local terminal output)
- `run_backtest.py` — current `backtest.py` (walk-forward simulation)

## Status
- Empty. `main.py` and `backtest.py` still at repo root during transition.
