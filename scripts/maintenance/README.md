# scripts/maintenance

One-time or infrequent maintenance scripts.

## Files (to be created during file migration phase)
- `refresh_schwab_token.py` — current `auth_schwab.py` (re-run when token expires ~7 days)

## Status
- Empty. `auth_schwab.py` still at repo root during transition.
