# docs/

Project documentation for the Adaptive Trading OS.

## Index

| File | Contents |
|---|---|
| `repo_inventory.md` | File-by-file assessment of the original repo (Phase 1 baseline) |
| `adaptive_trading_os_gap_analysis.md` | What exists vs. what the target OS needs (Phase 1 baseline) |
| `folder_architecture_plan.md` | Target `src/` layout and design principles |
| `security_findings.md` | Hardcoded secrets audit, `.gitignore` coverage |
| `refactor_log.md` | What moved, what was shimmed, known import risks per phase |
| `phase_1_to_7_review.md` | Full structured review after Phase 7 — defects, gaps, next steps |

## Notes

- `repo_inventory.md` and `adaptive_trading_os_gap_analysis.md` reflect the **Phase 1 baseline**
  state. For current architecture, see the README files inside each `src/` subdirectory.
- All docs here are for humans. Code-level contracts live in `src/core/contracts/`.
